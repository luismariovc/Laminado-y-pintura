<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LaminadoYPintura" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Optional: make navigation bar transparent or matching if desired, but focus is status bar -->
        <!-- <item name="android:navigationBarColor">@android:color/transparent</item> -->
    </style>

    <style name="Theme.LaminadoYPintura" parent="Base.Theme.LaminadoYPintura" />
</resources>